[project]
name = "hdl"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "cocotb>=2.0.1",
    "numpy>=2.4.4",
    "pytest>=9.0.3",
]

[build-system]
requires = ["uv-build>=0.12,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "hdl_tools"
module-root = ""

[dependency-groups]
dev = [
    "ruff>=0.16.1",
]
