SIM = questa

TOPLEVEL_LANG = verilog
TOPLEVEL = ch_fir

VERILOG_SOURCES += $(PWD)/../rtl/ch_fir.sv
VERILOG_SOURCES += $(PWD)/../rtl/ch_fir_coe.sv
VERILOG_SOURCES += $(PWD)/../rtl/ch_fir_stage.sv

MODULE = test_ch_fir

include $(shell cocotb-config --makefiles)/Makefile.sim
